import { StepComponentProps } from '@/stores/onboardingComponents'

export default function CycleAffectsMentalityStep({
  onContinue,
}: StepComponentProps) {
  return <div onClick={onContinue}>12</div>
}
